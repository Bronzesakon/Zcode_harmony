// Top-level build file. Plugin versions are declared here once and applied in
// the module build scripts, so the version catalog stays in one place.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.1.0" apply false
}
