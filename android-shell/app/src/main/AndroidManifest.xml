<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- QR scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 16 Live Updates (ColorOS 16 流体云). A normal, non-runtime
         permission: declaring it is what makes the app eligible at all. The
         actual promotion is opt-in per notification (see notify/LiveUpdate.kt)
         and the user can still switch it off in Settings. -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

    <!-- Android 13+ runtime permission for the task notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service. FOREGROUND_SERVICE is pre-14; the _SPECIAL_USE
         variant is required from Android 14 (API 34) on, and must match the
         service's android:foregroundServiceType below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Lets the user whitelist the app from Doze/battery optimisation so the
         WebSocket is not suspended while backgrounded (§5.5). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Tablets without a camera must still be able to install. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".ZcodeRemoteApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ZcodeRemote"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- The launcher long-press menu (重新扫码 / 打开设置). Static, so it
                 exists before the app has ever run, and it must be declared on
                 the MAIN/LAUNCHER activity. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ZcodeRemote" />

        <!-- Keeps the process (and therefore the WebView + its relay socket)
             alive while backgrounded, and owns the ongoing "running tasks"
             notification (§5.5). specialUse is deliberate: dataSync is capped
             at 6h/24h on Android 15, specialUse has no such cap. -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="维持在后台与用户自有桌面设备的远程控制长连接,以便持续接收任务状态并发送通知" />
        </service>

        <!-- Hands the exported diagnostics file to a share target (settings ->
             分享日志). Nothing else in the app is exposed through it. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
